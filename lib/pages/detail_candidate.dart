import 'package:flutter/material.dart';

class DetailCandidate extends StatelessWidget {
  const DetailCandidate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
